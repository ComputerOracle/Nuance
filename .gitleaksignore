# e2e/mock-wallet.ts's TEST_PRIVATE_KEY is Hardhat/Anvil's well-known,
# publicly-documented default Account #0 private key — used across
# countless open-source projects as a test-only signer, never associated
# with real funds. gitleaks' generic-api-key rule (correctly, in general)
# flags any 32-byte hex string assigned to a *_PRIVATE_KEY-shaped name;
# this one specific historical occurrence is a confirmed false positive,
# not a real leaked secret. Fingerprint from a real gitleaks v8.30.1 run
# against this repo's full history (`gitleaks git`), not guessed.
04f6c530e768c8e0783a57db4bec0ac55dd79d0d:e2e/mock-wallet.ts:generic-api-key:7
